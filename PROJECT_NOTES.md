# ملاحظات المشروع والتحديات وفرص التطوير — SecureGuide

> وثيقة حيّة لتوثيق أهم الملاحظات والتحديات والمخاوف، وأفكار وفرص التحسين والتطوير.
> آخر تحديث: 2026-07-11 · الحالة: مرحلة تصميم البيانات والوثائق (لا يوجد كود تطبيق بعد).

---

## 0. لمحة سريعة عن الحالة الراهنة
- **المصدر المرجعي القاطع للتصنيف:** USACM v2.2.1 و SDT v2.2.1.
- **نموذج التصنيف المعتمد:** USACM وSDT هما النواة الرسمية. أي جداول أو تصنيفات إضافية يجب أن تكون امتداداً منظماً فوقهما، لا بديلاً عنهما ولا تعديلاً عشوائياً لقيمهما الأساسية.
- **قاعدة البيانات (SQLite، offline-first):** خمس هجرات مُتحقَّق منها آلياً:
  - `001_initial_schema.sql` — النواة (20 جدولاً): إدخال + كتالوج مرجعي + 10 جداول فرعية + قوالب + طبقة تشغيلية.
  - `002_assets_indicators_embeddings.sql` — الأصول + مؤشرات التهديد + التضمين/كشف التكرار (16 جدولاً).
  - `003_reference_data.sql` — 37 جدول lookup مفهرس ثنائي اللغة (268 صفاً) مطابِق لقيود المخطط.
  - `004_curation_layer.sql` — طبقة الجودة والتنسيق: `staging_artifacts`، `curation_batches`، `consolidation_decisions`+`members` (6 قرارات دمج)، `curation_lessons`، `schema_migrations`.
  - `005_views.sql` — 6 عروض للتنسيق والمنتج (`v_review_queue`، `v_duplicate_candidates`، `v_catalog_curation`، `v_artifact_detail`، `v_profile_dashboard`، `v_gap_analysis`).
- **السياسات الحاكمة:** [`docs/CLASSIFICATION_POLICY.md`](docs/CLASSIFICATION_POLICY.md) (كيف نصنّف)، [`docs/AUTHORING_POLICY.md`](docs/AUTHORING_POLICY.md) (كيف نصوغ)، [`docs/CONSOLIDATION_POLICY.md`](docs/CONSOLIDATION_POLICY.md) (كيف نوحّد وندمج المتشابه)، [`docs/RELATIONSHIP_POLICY.md`](docs/RELATIONSHIP_POLICY.md) (كيف نربط).
- **البيانات الخام:** ~2,798 عنصراً في 21 كتالوجاً (`SecureGuide_Mobile_Docs/Raw_Catalogs/`) — المجموعة الحالية كافية للـMVP، وكلها **مسودّات قبل التصنيف** وليست محتوى نهائياً للنشر. الاستيراد مستقبلاً سيكون نادراً، والأولوية الآن لمعالجة هذه المجموعة وترقيتها.
- **اتجاه المحتوى المعتمد:** لا يلزم الاحتفاظ بالنص الكامل للضوابط في الكتالوج النهائي؛ الأفضل صياغة عناصر SecureGuide موحدة بالإنجليزية، مع حفظ النسب والمراجع وإتاحة العربية لاحقاً كترجمة اختيارية.
- **اتجاه المنتج المعتمد:** الأولوية للمرونة والعملية وسهولة التصحيح، لا لبناء نظام مؤسسي ثقيل قائم على رقابة مشددة أو workflows معقدة.
- **أدوات التحقق:** `scripts/validate_schema*.py`، `validate_reference_data.py`، `validate_curation.py` (الأربعة تمرّ).
- **التوجيه المعتمد (من `SECUREGUIDE_AI_AGENT_BRIEF.md`):** التركيز الحالي على بناء **Quality & Curation Layer** لمعالجة الكتالوج (تنظيف/صياغة/تصنيف/توحيد) قبل أي ميزات واسعة. أُنجزت الطبقة السكيمائية والعروض؛ تبقّى سكربتات المعالجة (أدناه I2/I3/الـWorkbook).

---

## 1. أهم الملاحظات (Notes)

| # | الملاحظة | الأثر |
|---|---|---|
| N1 | المشروع في طور التصميم؛ لا يوجد كود تطبيق فعلي بعد — المخطط لم يُختبَر ضد استعلامات واجهة حقيقية. | متوسط |
| N2 | نسخ USACM/SDT مكرّرة ثلاث مرات (`reference/`, `_Archive/`, `SecureGuide_Mobile_Docs/`). وُحِّد محتواها، لكن التكرار قائم ويعرّض للانحراف مجدداً. | متوسط |
| N3 | `_Archive/` مرجع تاريخي فقط ولا يُعتمد عليه (بقرار المالك). | منخفض |
| N4 | تصنيف الإطار (MITRE tactic، CSF function، NCA domain…) مدفون داخل `context_paragraph` **متعدد الشكل** (JSON أحياناً ونص حر أحياناً) — لا كأعمدة مهيكلة. هذا يدعم الحاجة إلى مرحلة staging لمعالجة المجموعة الحالية، لا إلى نظام استيراد عام ومعقد منذ البداية. | عالٍ للمعالجة |
| N5 | القيم التشغيلية على `security_artifacts` هي **قيم افتراضية مرجعية فقط**؛ الحقيقة في `profile_artifacts`. يجب أن تحترم طبقة التطبيق هذا الفصل. | عالٍ |
| N6 | `Doc 03` يصف مخططاً أوسع/طموحاً (أصول/مؤشرات) لا يطابق الهجرات بالكامل — أُضيفت لافتة "المصدر القاطع = الهجرة" لكن التوثيق لم يُعَد بناؤه كلياً. | متوسط |
| N7 | القيود (`CHECK`) وجداول الـ`lk_` مصدران منفصلان للقيم، مُبقيان متطابقين عبر مولّد + محقّق (وليس عبر FK). | متوسط |
| N8 | الاستيراد لم يعد نشاطاً متكرراً في المدى القريب؛ البيانات المطلوبة جُمعت بالفعل. هذا يخفض أولوية بناء import framework عام، ويرفع أولوية batch processing والتصنيف والدمج والمراجعة. | متوسط |
| N9 | يمكن أن تظهر حاجة عملية لإضافات على USACM/SDT، لكن الإضافة يجب أن تتم كامتداد SecureGuide موثق ومفصول عن النواة الرسمية. | متوسط |

---

## 2. التحديات (Challenges)

- **C1 — معالجة وتصنيف 2,798 عنصراً موجوداً يدوياً/بمساعدة AI:** هذا هو الجهد الأساسي. يتطلب تطبيق [`CLASSIFICATION_POLICY.md`](docs/CLASSIFICATION_POLICY.md) على كل عنصر (نوع/مستوى/مجال/إلزام + حقول خاصة)، مع توجيه منخفض الثقة (≤0.70) لمراجعة بشرية.
- **C2 — التكرار الكثيف المتوقع:** أطر متداخلة (NIST CSF مرتين، ATT&CK مرتين، وتقاطع CIS/ISO/NIST/PCI). لم يعد الهدف تحويل كل سجل خام إلى عنصر نهائي مستقل، بل اكتشاف التشابه ودمج الضوابط المتقاربة في عناصر SecureGuide موحدة مع حفظ كل المراجع.
- **C3 — صياغة المحتوى (لغوياً):** البدء باعتماد اللغة الإنجليزية كلغة canonical كاملة في الـMVP. الحقول العربية تبقى اختيارية/مؤجلة، وتُعامل الترجمة لاحقاً كطبقة إثراء لا كشرط لقبول العنصر.
- **C4 — أداء الجوال:** متطلب <100ms مع 10,000+ عنصر — يستلزم فهرسة دقيقة + FTS، واختباراً فعلياً.
- **C5 — نموذج التضمين على الجهاز:** في الـMVP يكفي تقييم نموذج إنجليزي/أمني جيد للتشابه وكشف التكرار. تقييم التضمين متعدد اللغات يصبح مهماً عند تفعيل الترجمة العربية والبحث العربي.
- **C6 — تلوّث الترميز في المصدر:** أحرف mojibake (`�`) في بعض ملفات Raw_Catalogs، وتلف backtick سابق في الوثائق (أُصلح). يجب تنظيف الاستيراد.
- **C7 — بوابة المراجعة البشرية قد تصبح عنق زجاجة** إن كثُرت العناصر منخفضة الثقة أو الغامضة (Legacy).

---

## 3. المخاوف والمخاطر (Concerns / Risks)

| # | الخطر | الخطورة | التخفيف المقترح |
|---|---|---|---|
| R1 | **حقوق الملكية الفكرية للأطر المستوردة** (ISO 27002، CIS، PCI وغيرها). إعادة توزيع النص الكامل قد يكون غير مسموح حسب الترخيص. | **عالية** | **تخفيف محوري:** لا يشحن المنتج كتالوجاً يحتوي النصوص الأصلية الكاملة للأطر المحمية. الكتالوج النهائي يحتوي عناصر SecureGuide موحدة ومُعاد صياغتها، مع source IDs ومراجع ومعلومات نسب. عند الحاجة إلى النص الأصلي، يستورده المستخدم محلياً من مصدر يملك حق استخدامه. |
| R2 | **زحف النطاق:** PRD/UI تعرّف سطحاً ضخماً (10 أقسام إعدادات، مزامنة، مشاركة، مؤشرات) بينما AGENTS.md يؤجّل أغلبه لِما بعد MVP. | عالية | الالتزام بنطاق MVP؛ بناء الطبقات الإضافية كهجرات لاحقة عند الحاجة. |
| R3 | **تناقض المزامنة/المشاركة مع مبدأ Offline-first للفرد** في النسخة الأولى. | متوسطة | تأجيلها صراحةً؛ عدم بناء جداولها الآن. |
| R4 | **إساءة استخدام الحقول المرجعية الافتراضية** كأنها حالة عالمية للمؤسسة. | متوسطة | إبقاء الفصل واضحاً في الواجهة وطبقة الخدمة، دون إضافة رقابة مؤسسية معقدة. |
| R5 | **انحراف القيم** بين قيود `CHECK` وجداول `lk_` مستقبلاً. | متوسطة | تحويل القيود إلى FK على `lk_` (فكرة I1)، أو إبقاء المحقّق في CI. |
| R6 | **معايرة الثقة (0.70) اعتباطية** بلا بيانات. | متوسطة | ضبط العتبة تجريبياً على مجموعة موسومة. |
| R7 | **درجات المخاطر/الأولوية بلا صيغة صريحة** قد تُعرض (يمنعها AGENTS.md). | متوسطة | عدم عرض أي درجة قبل توثيق صيغتها ومدخلاتها. |
| R8 | **المخطط غير مُثبت ضد الواجهة** (لا كود بعد). | متوسطة | بناء استعلامات/عروض (Views) نموذجية مبكراً. |
| R9 | `tests/fixtures/expected/` فارغ؛ و`secureguide_test.db` (أثر بناء قديم) متروك في الجذر. | منخفضة | إنشاء مجموعة اختبار ذهبية؛ حذف/تجاهل ملف الـDB. |
| R10 | محلّل الـ IDE يعامل SQLite كـ T-SQL فيُظهر أخطاء كاذبة (`PRAGMA`, `GLOB`, `CHECK` مضمّن). | منخفضة | الاعتماد على محقّق SQLite الفعلي؛ تجاهل تحذيرات المحرّر. |

---

## 4. أفكار وفرص التحسين والتطوير (Ideas & Opportunities)

### أولوية عالية (تُمكّن الـMVP)
- **I1 — تحويل قيود `CHECK` إلى مفاتيح خارجية على جداول `lk_`:** مصدر واحد للتحقق والعرض معاً (يتطلب إنشاء الـlookups قبل الجداول الرئيسية).
- **I2 — سكربت معالجة وترقية المجموعة الحالية (`raw_artifacts` → staging → `security_artifacts`):** يطبّق منهجية التصنيف آلياً/بمساعدة AI على البيانات الموجودة، ويقترح إعادة صياغة إنجليزية موحدة، ويحفظ النسب (`promoted_artifact_id`/mappings)، ويوجّه منخفض الثقة للمراجعة. لا يلزم في هذه المرحلة بناء مستورد عام لكل صيغ مستقبلية محتملة.
- **I3 — سكربت كشف التشابه والتوحيد + تكامل `sqlite-vec`:** يولّد المتجهات بعد الصياغة الإنجليزية، ويقترح دمج عدة ضوابط مصدرية في عنصر SecureGuide واحد أو مجموعة تكافؤ، دون حذف السجلات الخام أو فقدان lineage.
- **I4 — جدول FTS5 افتراضي:** بحث نصي سريع (مطلب <100ms) مع دمج البحث الدلالي.
- **I5 — عروض (Views) جاهزة:** لوحة الملف، تحليل الفجوات، العناصر منخفضة الثقة، التكرارات المعلّقة.

### أولوية متوسطة (جودة ونضج)
- **I6 — محرّك التكافؤ (Equivalence clustering):** طيّ الـ2,798 عنصراً إلى مجموعة مفاهيم قانونية (`equivalence_groups`) لتقليل الضجيج.
- **I7 — اقتراح crosswalks آلياً:** ترشيح `framework_mappings` من أزواج التشابه العالي عبر الأطر (بقوة DIRECT/INDIRECT/PARTIAL/INFORMATIVE + تبرير). عند دمج عدة مصادر في عنصر موحد، تصبح الـmappings هي الدليل على نسب العنصر لكل إطار.
- **I8 — جدول نسخ المخطط (`schema_migrations`) + مُشغّل هجرات** بسيط ومتسلسل.
- **I9 — قواعد جودة الاستيراد (USACM VAL-001…022):** فحوص آلية عند الإدخال قبل النشر.
- **I10 — مجموعة اختبار ذهبية:** تعبئة `tests/fixtures/expected/` بمخرجات تصنيف متوقعة (حوكمي/تقني/غامض/تكرار).
- **I11 — توحيد نسخة USACM/SDT في نسخة واحدة قانونية** وجعل البقية إشارات إليها (تفادي الانحراف — N2).
- **I12 — سجل تغييرات بسيط ومفيد:** حفظ آخر تعديل مهم وسبب مختصر عند التصحيح، دون بناء audit workflow مؤسسي ثقيل.

### أولوية لاحقة (توسّع)
- **I13 — طبقة إعدادات التطبيق (هجرة 004):** السياق المؤسسي، المستخدمون/الأدوار، الإشعارات، إعدادات AI — عند الاقتراب من الواجهة.
- **I14 — محرّك التقدّم/النضج بصيغة موثّقة:** حساب نسب التطبيق والفجوات من حالة الملف فقط.
- **I15 — مساعد صياغة بالـLLM:** توليد الحقول الإنجليزية أولاً وفق [`AUTHORING_POLICY.md`](docs/AUTHORING_POLICY.md) بعد تحديثها لتعكس قرار English-first. الترجمة العربية تُنفذ لاحقاً كإثراء منفصل مع مراجعة جودة.
- **I16 — المزامنة/المشاركة/التعاون:** بعد استقرار MVP (كما يوصي AGENTS.md).

---

## 5. ما يترتب على هذا التحديث

1. **يتغير هدف العمل على البيانات:** لم يعد الهدف بناء مسار استيراد متكرر أو نشر نصوص الضوابط الأصلية كما هي، بل معالجة المجموعة الحالية وتحويلها إلى عناصر SecureGuide canonical قابلة للتصنيف والبحث والتقييم.
2. **تُضاف مرحلة staging مهمة ومحدودة النطاق:** `raw_artifacts` يحفظ الأصل والمرجع، ومرحلة staging تستخرج الحقول وتكشف التشابه من البيانات الموجودة، ثم `security_artifacts` يستقبل الصياغة الموحدة المعتمدة فقط.
3. **الدمج يصبح سلوكاً مقصوداً:** يمكن لعدة ضوابط من CIS/NIST/ISO/PCI وغيرها أن تؤدي إلى عنصر SecureGuide واحد، بشرط حفظ كل source ID والنسخة والقسم وقوة المطابقة في `framework_mappings` أو علاقات/مجموعات تكافؤ.
4. **لا حذف ولا فقدان نسب:** الدمج لا يعني إزالة السجلات الخام. السجل الخام يبقى traceable، والعنصر الموحد يشير إلى مصادره.
5. **تنخفض مخاطر الترخيص في المنتج النهائي:** المنتج لا يحتاج إلى شحن النصوص الأصلية الكاملة للأطر المحمية. أي نص مصدر كامل يبقى محلياً عند المستخدم أو ضمن طبقة raw غير مخصصة للنشر، حسب الترخيص.
6. **تتغير متطلبات الصياغة:** حقول `_en` تصبح الإلزامية في MVP، وحقول `_ar` تبقى nullable/اختيارية إلى حين بناء مسار ترجمة ومراجعة عربي.
7. **إبقاء الوثائق الحاكمة متسقة:** تم تحديث [`docs/AUTHORING_POLICY.md`](docs/AUTHORING_POLICY.md) و[`docs/DATA_DICTIONARY.md`](docs/DATA_DICTIONARY.md)، وإضافة [`docs/CONSOLIDATION_POLICY.md`](docs/CONSOLIDATION_POLICY.md)، لتوضيح English-first، وإعادة الصياغة، وتقليل الاعتماد على `source_quote`، وتعريف الفرق بين source reference وcanonical rewritten content.
8. **تتغير أولوية الاختبارات:** يجب أن تختبر مجموعة الـgolden fixtures حالة دمج عدة مصادر في عنصر واحد، وحالة عنصر لا يجوز نشر نصه الأصلي، وحالة عنصر إنجليزي بلا ترجمة عربية.
9. **تصبح مراجعة الجودة أهم من الترجمة:** مراجعة النوع والمجال والالتزام والتشابه والثقة تأتي قبل تحسين اللغة العربية.
10. **تتأجل قدرات البحث العربي:** البحث العربي والترجمة والتضمين متعدد اللغات تصبح مراحل لاحقة، بينما يبدأ الـMVP ببحث إنجليزي موثوق وسريع.
11. **تنخفض أولوية مرونة الاستيراد المستقبلية:** لا حاجة الآن لدعم عشرات صيغ الإدخال أو واجهات استيراد متقدمة. المطلوب أداة batch قابلة للإعادة على المجموعة الحالية، مع إمكانية إضافة مستوردات لاحقة عند ظهور مصدر جديد فعلاً.

## 6. التوصية التنفيذية المجمعة

### 6.1 الفكرة المركزية

أفضل مسار لـSecureGuide في هذه المرحلة هو:

> **تحويل المجموعة الخام الموجودة إلى كتالوج SecureGuide موحد، إنجليزي، قابل للتصنيف والبحث والتقييم، مع حفظ النسب الكامل لكل مصدر، بدلاً من بناء منصة استيراد واسعة أو نشر نصوص الضوابط الأصلية.**

هذا يعني أن القيمة الأساسية للـMVP ليست في كثرة الاستيراد، بل في جودة المعالجة: التصنيف الصحيح، التوحيد، الدمج، المراجعة، وإظهار الحالة التشغيلية داخل profiles.

### 6.2 خط المعالجة الموصى به

المسار العملي المقترح:

```text
raw_artifacts
  → staging / parsed source records
  → English canonical draft
  → USACM + SDT classification
  → embedding similarity and duplicate candidates
  → equivalence / mapping suggestions
  → human review queue
  → approved security_artifacts
  → FTS5 search + indexes + profile views
```

### 6.3 الحلول الموصى بها

1. **معالجة المجموعة الحالية بدلاً من بناء مستورد عام**
   بناء أداة batch قابلة للإعادة على الكتالوجات الموجودة الآن. أي مستوردات مستقبلية تُضاف فقط عند وجود مصدر جديد فعلي.

2. **إعادة الصياغة بدلاً من نسخ النص**
   لا يتم نشر النصوص الأصلية الكاملة في الكتالوج النهائي. يتم توليد عناصر SecureGuide canonical باللغة الإنجليزية، مع حفظ source catalog/version/section/raw ID.

3. **الدمج والتوحيد كجزء من التصميم**
   عند وجود عدة ضوابط متشابهة من مصادر مختلفة، الأفضل إنشاء عنصر SecureGuide واحد أو مجموعة تكافؤ، مع تسجيل كل mapping وقوته وتبريره.

4. **English-first للـMVP**
   الحقول الإنجليزية هي شرط الاعتماد. العربية تبقى اختيارية ومؤجلة كطبقة ترجمة/إثراء لاحقة.

5. **Core + Extensions للتصنيف**
   تبقى USACM وSDT هي التصنيف الرسمي المستخدم في البحث والتقارير والقيود. إذا ظهرت حاجة لا يغطيانها، تضاف كامتداد SecureGuide منفصل مثل tags، aliases، extension labels، source-specific categories، أو جداول مساعدة، مع ربطها بالقيمة الرسمية الأقرب.

6. **التضمين للمعالجة، وFTS5 للمنتج**
   يستخدم التضمين مبكراً لاكتشاف التشابه والتكرار بعد توليد النص الإنجليزي المنظف. أما FTS5 فيُبنى لاحقاً فوق الكتالوج المعتمد لتجربة البحث اليومية السريعة.

7. **مراجعة بشرية مركزة**
   لا تُراجع كل العناصر بنفس العمق، ولا نحتاج workflow موافقات معقد. تُعطى الأولوية للعناصر منخفضة الثقة، والعناصر المرشحة للدمج، والتصنيفات الغامضة، والمصادر التي قد تحمل قيود ترخيص.

8. **Views مبكرة قبل الواجهة الكاملة**
   بناء views للوحة profile، gap analysis، عناصر المراجعة، التكرارات، والـmappings المقترحة قبل بناء واجهة كبيرة. هذا يختبر المخطط عملياً بسرعة.

9. **اختبارات ذهبية صغيرة لكنها حاسمة**
   إنشاء مجموعة fixtures تشمل: عنصر حوكمي، إعداد تقني، عنصر غامض، ضوابط متشابهة من عدة مصادر، وعنصر إنجليزي بلا ترجمة عربية.

10. **عدم عرض risk scoring مبكر**
   لا يتم عرض درجات خطر أو نضج إلا بعد توثيق الصيغة والمدخلات. في الـMVP يكفي progress/gaps من حالات profile الفعلية.

11. **تحديث السياسات لتطابق القرار**
    تم تثبيت [`docs/AUTHORING_POLICY.md`](docs/AUTHORING_POLICY.md) للـEnglish-first وإعادة الصياغة، وإضافة [`docs/CONSOLIDATION_POLICY.md`](docs/CONSOLIDATION_POLICY.md) للتوحيد والدمج والتعلم من الدروس، وتحديث [`docs/DATA_DICTIONARY.md`](docs/DATA_DICTIONARY.md) ليتسق مع ذلك.

12. **تصحيح التصنيفات بسهولة**
    إذا ظهر تصنيف غير مقنع، يجب أن يتمكن المستخدم من تعديله مباشرة من القيم المعتمدة، مع سبب اختياري ومؤشر بسيط أن التصنيف عدله الإنسان. لا حاجة إلى مسار موافقات أو صلاحيات مؤسسية معقدة في الـMVP.

13. **تصدير/استيراد للمراجعة الخارجية**
    توفير ملف Excel/CSV منظم يمكن عرضه على وكيل ذكاء اصطناعي أو مراجع بشري لتحسين الصياغة والتصنيفات واقتراح قيم إضافية، ثم استيراده مرة أخرى إلى التطبيق عبر مرحلة تحقق ومعاينة قبل الاعتماد.

### 6.4 مسار التصدير والاستيراد للتحسين الخارجي

الفكرة المقترحة ليست تصدير قاعدة البيانات حرفياً بكل علاقاتها الداخلية، بل تصدير **Curation Workbook** مبسط ومفهوم يمثل الكتالوج المراد تحسينه.

#### الهدف

تمكين المستخدم من إخراج كامل عناصر الكتالوج أو عينة محددة إلى Excel/CSV، ثم استخدام وكيل ذكاء اصطناعي أو مراجعة بشرية لتحسين:

- العناوين والتعريفات الإنجليزية.
- نوع USACM.
- مستوى التجريد.
- المجال والمجال الفرعي SDT.
- الثقة والتبرير.
- tags والسياقات.
- اقتراح الدمج أو التكافؤ بين عناصر متشابهة.
- اقتراح قيم إضافية كـtags أو aliases أو source labels.

#### شكل التصدير المقترح

في Excel يكون الأفضل استخدام عدة sheets:

1. `artifacts`
   العناصر الأساسية المراد تحسينها، مع stable IDs لا تتغير.

2. `mappings`
   مراجع المصادر والإطارات لكل عنصر.

3. `tags`
   tags الحالية والمقترحة.

4. `relationships`
   علاقات أو اقتراحات دمج/تكافؤ.

5. `lookups`
   القيم المعتمدة لـUSACM وSDT وtag types وmapping strengths، حتى لا يخترع الوكيل قيماً غير صحيحة.

6. `proposed_values`
   أي قيم جديدة يقترحها الوكيل، مثل tag جديد أو alias أو تسمية مصدر. هذه القيم لا تُضاف تلقائياً إلى النموذج الأساسي.

في CSV يمكن دعم ملف flat واحد للسيناريو البسيط، لكن Excel أفضل لأنه يحافظ على العلاقات بدون تعقيد زائد.

#### قواعد الاستيراد الراجع

1. **لا اعتماد مباشر**
   الملف المستورد يدخل إلى staging/import review، ولا يكتب مباشرة فوق `security_artifacts`.

2. **stable ID إلزامي**
   كل صف يجب أن يحمل `artifact_id` أو `raw_artifact_id` أو `curation_row_id` ثابتاً حتى يعرف التطبيق ماذا تغيّر.

3. **تمييز القيم الجديدة**
   أي قيمة غير موجودة في USACM/SDT لا تُقبل كتصنيف أساسي تلقائياً. تُسجل كاقتراح أو tag/alias حسب نوعها.

4. **تقرير فروقات قبل الاعتماد**
   يعرض التطبيق ما تغير: العنوان، التعريف، النوع، المجال، tags، mappings، واقتراحات الدمج.

5. **تحقق قبل الحفظ**
   نفس قواعد التصنيف تطبق على الملف المستورد: توافق `sub_domain` مع `primary_domain`، صحة `type`، وجود rationale عند الحاجة، وعدم فقدان lineage.

6. **اعتماد انتقائي**
   يستطيع المستخدم قبول كل التعديلات أو قبول عناصر محددة فقط.

7. **حفظ نسخة احتياطية**
   قبل تطبيق التعديلات على الكتالوج المعتمد، يحفظ التطبيق snapshot أو export سابق يمكن الرجوع إليه عملياً، بدون بناء نظام تدقيق مؤسسي ثقيل.

#### حدود مهمة

- هذا المسار مخصص لتحسين الكتالوج والتصنيفات، وليس لتعديل حالات المؤسسات التشغيلية إلا إذا صُمم ملف منفصل لذلك لاحقاً.
- لا يسمح الملف بإضافة نوع USACM أو مجال SDT جديد مباشرة. الإضافات المرنة تكون tags أو aliases أو ملاحظات أو اقتراحات.
- النصوص الأصلية للمصادر لا تُعاد نشرها داخل الملف إلا إذا كان المستخدم يملك حق استخدامها محلياً.

### 6.5 ضمان صحة التصنيفات

لضمان أن كل عنصر أمني مصنف بشكل صحيح دون تقييد زائد، يعتمد SecureGuide على مجموعة آليات خفيفة لكنها قوية:

1. **قواعد تصنيف واضحة**
   تطبيق [`CLASSIFICATION_POLICY.md`](docs/CLASSIFICATION_POLICY.md) كمرجع مباشر: ما نوع العنصر؟ ما مستواه؟ ما المجال الأساسي؟ ما المجال الفرعي؟

2. **قوائم اختيار مقيدة للقيم الأساسية**
   لا يكتب المستخدم `type` أو `domain` كنص حر. يختار من USACM وSDT فقط. هذا يمنع الأخطاء الإملائية والقيم العشوائية.

3. **تحقق تلقائي من توافق المجال الفرعي**
   أي `sub_domain` يجب أن يتبع `primary_domain`. لا يسمح النظام مثلاً بربط `SD-03.04` مع `SD-04`.

4. **درجة ثقة وتبرير مختصر**
   كل تصنيف مولد آلياً يحمل `classification_confidence` و`classification_rationale`. العناصر منخفضة الثقة تظهر في قائمة مراجعة بسيطة.

5. **أمثلة ذهبية للمقارنة**
   تجهيز عينة صغيرة معتمدة من عناصر مصنفة بشكل صحيح: governance، technical configuration، evidence، risk، ambiguous، وduplicate. تستخدم هذه العينة لاختبار أي تعديل في قواعد التصنيف.

6. **اقتراح بدائل عند الغموض**
   عند العنصر الغامض، لا يفرض النظام إجابة واحدة بثقة زائفة. يعرض التصنيف المقترح وبديل أو بديلين مع السبب، ثم يترك القرار للمستخدم.

7. **فحص تناقضات بسيط**
   سكربت تحقق يبحث عن الحالات الواضحة مثل: `ART-CFG` بلا إعداد قابل للفحص، أو `ART-EVD` بلا علاقة تحقق، أو mapping غير مباشر بلا rationale.

8. **تصحيح بشري مباشر**
   التصنيف الصحيح في النهاية ليس قرار AI وحده. يجب أن يكون تعديل النوع أو المجال أو المجال الفرعي سهلاً من شاشة العنصر، مع حفظ آخر تصحيح بشري.

9. **تغذية راجعة لقواعد التصنيف**
   التصحيحات المتكررة لا تتحول إلى رقابة، بل إلى تحسين عملي: إذا عدل المستخدم عشرات العناصر من `ART-CTR` إلى `ART-CFG` لنمط معين، تُحدّث قاعدة التصنيف أو prompt المعالجة.

10. **عدم توسيع التصنيف الأساسي عشوائياً**
    إضافة tags وسياقات مرنة مسموحة وسهلة. أما إضافة نوع USACM أو مجال SDT جديد فلا تتم كقيمة عادية داخل العنصر؛ تُسجل أولاً كـSecureGuide extension أو proposed taxonomy change، مع ربطها بأقرب قيمة رسمية حتى لا تنكسر الفلاتر والتقارير.

11. **ربط كل امتداد بالنواة**
    أي تصنيف إضافي يجب أن يجيب عن سؤالين: ما القيمة الرسمية الأقرب في USACM/SDT؟ ولماذا لا تكفي وحدها؟ إذا لم توجد إجابة واضحة، فالأغلب أن الإضافة يجب أن تكون tag أو alias لا تصنيفاً أساسياً جديداً.

12. **اختبار أثر الإضافة قبل اعتمادها**
    قبل تحويل أي امتداد إلى تصنيف أساسي، يجب اختبار أثره على البحث، views، Curation Workbook، التقارير، وقواعد التحقق. في معظم الحالات، الإبقاء عليه كامتداد مرن أفضل من تعديل النواة.

### 6.6 ترتيب التنفيذ المقترح

1. تحديث قاموس البيانات ليتسق مع سياسة الصياغة الحالية: English-first وcanonical rewrite.
2. إنشاء staging model أو staging tables لمعالجة المجموعة الحالية.
3. بناء batch processor يقرأ `raw_artifacts` ويولّد مسودات canonical.
4. تطبيق تصنيف USACM/SDT مع confidence/rationale/review status.
5. توليد embeddings للنصوص الإنجليزية المنظفة واقتراح التكرارات/التكافؤ.
6. إنشاء review queue للعناصر منخفضة الثقة والدمج المقترح.
7. إنشاء نموذج extensions بسيط للقيم المقترحة التي لا تغطيها USACM/SDT مباشرة، مع ربطها بالقيم الرسمية الأقرب.
8. بناء export/import curation workbook لتحسين العناصر خارج التطبيق ثم إرجاعها إلى staging.
9. ترقية العناصر المعتمدة إلى `security_artifacts` مع mappings وlineage.
10. بناء FTS5، الفهارس، والـviews فوق الكتالوج المعتمد.
11. إنشاء profiles وprofile_artifacts وربط الحالة التشغيلية بالملف لا بالكتالوج.
12. بناء dashboard/report بسيط من profile state فقط.

### 6.7 ما يجب تأجيله

- import platform عام ومتعدد الصيغ.
- الترجمة العربية الشاملة.
- البحث العربي والتضمين متعدد اللغات.
- المزامنة والمشاركة والتعاون.
- risk scoring أو maturity scoring قبل توثيق الصيغة.
- واجهة ضخمة قبل اختبار الـviews والاستعلامات الأساسية.
- workflows مؤسسية ثقيلة للموافقات، الأدوار، التدقيق، والتصعيد.

## 7. قرارات تصميمية مثبّتة (مرجع سريع)
- الأصول/المخاطر/التهديدات/الثغرات = أنواع `ART-AST/RSK/THR/VUL` مرتبطة بالعلاقات؛ والأصول **الفعلية للمؤسسة** في `enterprise_assets` (تشغيلي).
- اسم عمود العلاقة المعياري = `relation_type`.
- كشف التكرار **بشري في الحلقة** ولا يحذف المصدر (AGENTS.md قاعدة 12).
- الحالات الأربع (تنفيذ/تحقق/فعالية/استثناء) لا تُدمج أبداً في واحدة.
- **إدارة المحتوى وحقوق النشر (R1):** التخلي عن فكرة شحن النصوص الأصلية الكاملة للأطر داخل الكتالوج النهائي؛ واستبدالها بإعادة صياغة موحدة ودمج الضوابط المتشابهة من مصادر متعددة في عناصر SecureGuide canonical.
- **لغة النظام (C3):** البدء باللغة الإنجليزية كلغة canonical كاملة في المرحلة الحالية، وتأجيل الترجمة للعربية كخيار إثراء مستقبلي.
- **النسب قبل الاقتباس:** المرجعية القانونية والتدقيقية تتحقق عبر source catalog/version/section/raw ID/mapping strength/rationale، لا عبر نشر اقتباسات مطولة من النص الأصلي.
- **الاستيراد نادر والمعالجة هي الأولوية:** بما أن الكتالوجات الأساسية جُمعت بالفعل، تركيز الـMVP يكون على batch processing، التوحيد، التصنيف، المراجعة، والبحث، لا على بناء import platform واسع.
- **المرونة قبل التعقيد المؤسسي:** التصحيح اليدوي للتصنيفات يجب أن يكون سهلاً، والتحقق يجب أن يكون ذكياً وخفيفاً، دون تحويل الـMVP إلى نظام موافقات ومراقبة ثقيل.
- **Curation Workbook:** يمكن تحسين الكتالوج خارج التطبيق عبر Excel/CSV ثم إرجاعه إلى staging مع validation ومعاينة فروقات، بدلاً من إجبار المستخدم على كل التصحيحات داخل الواجهة.
- **USACM/SDT Core + SecureGuide Extensions:** النواة الرسمية للتصنيف تبقى USACM وSDT. الإضافات مسموحة عند الحاجة، لكنها تحفظ كامتدادات موثقة ومربوطة بالنواة، ولا تستبدل القيم الأساسية إلا بقرار تحديث منظم.
- **سياسات المحتوى:** الصياغة تضبطها `AUTHORING_POLICY.md`، والتوحيد/الدمج تضبطه `CONSOLIDATION_POLICY.md`، والتضمين يبقى أداة ترشيح لا قراراً نهائياً.

---

*للمساهمة: أضِف ملاحظاتك تحت القسم المناسب مع تاريخها، وحدّث "آخر تحديث" أعلى الملف.*
