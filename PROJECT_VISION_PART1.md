# تصور مشروع SecureGuide: قاعدة معرفة أمنية موحدة ومنصة تقييم مؤسسي

**الإصدار:** 3.1 (تحديث منقح ومضبوط معمارياً)
**الهدف:** تلخيص الرؤية التنفيذية، طبقات نموذج البيانات، والفلسفة المعمارية للمشروع.

---

## 1. صياغة الفكرة والرؤية الأساسية

الفكرة المقترحة هي بناء منصة (Mobile/Desktop) تجمع الكيانات والعناصر الأمنية من المعايير العالمية والأطر والإرشادات في قاعدة معرفة واحدة، ثم تحول كل عنصر إلى "سجل موحد" (Unified Record) قابل للتصنيف، الربط، الفلترة، والتقييم المؤسسي.

المشروع ليس مجرد مكتبة نصوص أو مستندات أمنية، بل **منصة تشغيل معرفية (Knowledge Operations Platform)** تساعد المستخدم على الإجابة عن أسئلة عملية دقيقة:
- ما العناصر الأمنية المناسبة لنشاط مؤسستي بالتحديد؟
- ما الضوابط أو المتطلبات التي تنطبق على بيئتي؟
- ما حالة تطبيق كل ضابط على أرض الواقع؟
- ما هي أولوياتنا وأين تكمن الفجوات الحرجة؟
- ما الأدلة المطلوبة لإثبات التطبيق للمدققين؟
- كيف ترتبط هذه الضوابط بالأصول، المخاطر، والمعايير الأخرى؟

**الصياغة المختصرة للرؤية:**
> **SecureGuide** هو نظام مرجعي وتقييمي (Offline-First) يجمع المعايير والضوابط والإرشادات الأمنية في كتالوج مركزي موحد، ويتيح للمؤسسات استنساخ قوالب مخصصة لسياقها لتقييم حالة التطبيق، وإدارة الأصول، واكتشاف الفجوات بناءً على تصنيف (USACM) ومصادر معرفة وتهديدات قابلة للتحديث والتحقق.

---

## 2. الفكرة المحورية في التصميم (القرار المعماري الأهم)

أهم قرار تصميمي تم اعتماده هو **الفصل بين الكتالوج المرجعي والحالة التشغيلية**، مع السماح بقيم افتراضية مرجعية (مثل: مستوى الأولوية الافتراضي) قابلة للتخصيص والتجاوز داخل الملف المؤسسي:

1. **الكتالوج المرجعي (Master Catalog - security_artifacts)**  
   يحتوي على العناصر الأمنية الأصلية (الخام والمصنفة). هذا الكتالوج **ثابت ومرجعي**، يصف ماهية العنصر ولا يحتوي على حالة تطبيق مؤسسية.

2. **السياق التشغيلي (Enterprise Profile - profile_artifacts)**  
   يمثل مؤسسة، فرع، نظام، بيئة Cloud، أو مشروع تدقيق. **هنا فقط** نقوم بتخزين الحالة التشغيلية الفعّالة (مطبق/غير مطبق)، النتيجة، الأدلة، الملاحظات، والاستثناءات.

---

## 3. محتوى الكتالوجات الخام (Raw Catalogs)

بناءً على التجميع في مجلد Raw_Catalogs، يغطي النظام أكثر من **16 إطار عمل عالمي**.
**الاستنتاج المعماري:** العناصر ليست من مستوى واحد. بعضها متطلب (Requirement)، بعضها ضابط (Control)، بعضها إعداد تقني (Configuration)، وبعضها دليل (Evidence). لذلك، **يتم تصنيفها كأنواع متعددة بدلاً من اعتبارها كلها Controls**.

---

## 4. نموذج التصنيف الأساسي (USACM v2.2.1)

كل عنصر أمني يجب أن يُصنّف عبر أبعاد مستقلة وقيود صارمة:

| البعد (Dimension) | الغرض |
|---|---|
| **Artifact Type** | يحدد ماهية العنصر: Requirement, Control, Standard, Configuration, Evidence, Policy... |
| **Abstraction Level** | مستوى التجريد: Governance, Risk, Control, Technical, Metrics |
| **Security Domain** | المجال الرئيسي حسب تصنيف (SDT v2.2.1): 8 مجالات رئيسية. |
| **Sub-Domain** | المجال الفرعي: 40 مجالاً فرعياً تحديداً. |
| **Source** | أصل الكيان: معيار، قانون، عقد، سياسة داخلية، أفضل ممارسة، خطر. |
| **Obligation Level** | إلزامي، مشروط، موصى به، اختياري. |
| **Granularity** | عام، تفصيلي، تنفيذي، تقني، قياسي. |
| **Tags (الوسوم)** | سياقات ثانوية للتكنولوجيا والتهديدات. لا تستخدم كبديل للمجال الرئيسي. |

---

## 5. تعريف المصطلحات والجداول المحورية (Terminology & Entities)

لضمان عدم الخلط بين الجداول أثناء التطوير، نعرّف الكيانات التالية:
- **artifact (security_artifact):** العنصر المرجعي الثابت في الكتالوج الموحد (مثال: نص الضابط من ISO 27002).
- **template_item:** قاعدة إدراج عنصر معين داخل قالب (يحتوي على شروط مثل: إلزامي، موصى به، سبب الإدراج).
- **profile_artifact:** النسخة المستنسخة والمخصصة من الـ artifact داخل بيئة مؤسسة محددة (السياق التشغيلي).
- **assessment (profile_assessments):** تقييم تاريخي محدد بوقت لحالة الـ profile_artifact (مثال: تقييم الربع الأول).
- **evidence:** الملفات أو الروابط أو النصوص التي تثبت صحة التقييم وترتبط بالـ assessment.

---

## 6. دورة حياة العنصر الأمني (Artifact Lifecycle)

يمر أي عنصر يتم إدخاله للنظام بالحالات التالية لضمان الجودة:
- **RAW:** تم استيراده من المصدر بصيغة JSON الخام ولم يُعالج.
- **NORMALIZED:** تم تنظيف العناوين والنصوص وإزالة التنسيقات الدخيلة.
- **CLASSIFIED:** تم منحه تصنيف USACM أولي.
- **NEEDS_REVIEW:** التصنيف يحتوي على تعارض أو نسبة ثقة منخفضة ويحتاج تدخلاً بشرياً.
- **APPROVED:** تمت مراجعته والموافقة عليه من قبل مدير المعرفة.
- **PUBLISHED:** أصبح متاحاً في الكتالوج المرجعي للمستخدمين.
- **DEPRECATED:** تم استبداله بإصدار أحدث أو إلغاؤه من المصدر.

---

## 7. طبقات نموذج البيانات (Data Model Layers)

### 7.1 طبقة المصادر والإدخال (Raw Layer)
لحفظ المصدر الخام: source_catalogs, raw_artifacts, import_batches.

### 7.2 طبقة الكتالوج المرجعي (Master Catalog Layer)
قلب المعرفة المعياري: security_artifacts, artifact_relationships, artifact_tags, framework_mappings.

### 7.3 طبقة القوالب (Templates Layer)
القوالب ليست مجرد قوائم ثابتة، بل تحتوي على "قواعد اختيار" (إلزامي/موصى به/اختياري)، سبب الإدراج، وشروط التطبيق. الجداول: assessment_templates, template_items.

### 7.4 طبقة الملفات المؤسسية (Enterprise Profiles Layer)
بيئة العمل التشغيلية: enterprise_profiles, profile_artifacts, profile_assessments, profile_evidence.

### 7.5 طبقة ذكاء الأصول والتهديدات (Asset Intelligence Layer)
طبقة مستقبلية (تم وضع أساساتها): تعتمد على معادلات دقيقة لحساب (Risk Score) بناءً على (criticality, exposure, implementation gap, threat relevance) لربط الأصول بالثغرات والضوابط.

---

## 8. خط أنابيب الإدخال والتصنيف (Intake Pipeline)

المسار الآلي للبيانات:
1. **Import:** قراءة ملفات JSON الخام.
2. **Normalize:** تنظيف وتنظيم النصوص.
3. **Classify:** تصنيف أولي عبر (قواعد صارمة + AI + تحقق Schema + مراجعة بشرية عند انخفاض الثقة).
4. **Tag:** استخراج وسوم التكنولوجيا.
5. **Equivalence Mapping (لا للدمج العشوائي):** بدلاً من دمج عناصر ISO و CIS وحذف أحدهما (Deduplication الخطر)، يتم إنشاء (canonical_concepts أو equivalence_groups) وربط العناصر بها للحفاظ على المعنى القانوني المرجعي لكل أصل.
6. **Map:** ربط العناصر ببعضها. قوة الربط (Mapping Strength) تحدد بـ: DIRECT, PARTIAL, INDIRECT, INFORMATIVE مع كتابة التبرير (Rationale).

---

## 9. منطق اختيار العناصر للمستخدم (Onboarding Wizard)

بدل البحث اليدوي في آلاف العناصر، يبدأ التطبيق بمعالج ديناميكي يسأل عن القطاع، الحجم، الأنظمة، والالتزامات.
**النتيجة:** يقوم النظام بتوليد **"مجموعة أولية مقترحة"** (Enterprise Profile) تحتوي على العناصر المنطبقة، مع إدراج عناصر مستبعدة أو غير منطبقة مبررة (لتمكين المدقق من مراجعة أسباب الاستبعاد لاحقاً).

---

## 10. محركات المنطق الأساسية (The Engines)

لغرض بناء النسخة الأولى (MVP)، نركز على المحركات الأساسية فقط وتأجيل البقية:
- **MVP Engines:**
  - Import Engine
  - Classification Engine
  - Search & Filter Engine
  - Template Engine
  - Assessment Engine
  - Integrity Engine
- **Future Engines (واجهات مؤجلة):** Recommendation Engine, Priority/Risk Engine.

---

## 11. قرارات تصميم حاسمة (Design Principles)

1. الفصل بين التعريف المرجعي والحالة التشغيلية مع إتاحة تجاوز القيم الافتراضية.
2. لا تفترض أن كل عنصر هو (Control).
3. لا تجعل الوسوم (Tags) بديلاً عن المجال الرئيسي (Domain).
4. استخدم جدولاً مخصصاً للعلاقات يحدد قوة الربط (Mapping Strength).
5. احفظ العنصر الخام دائماً كمرجع للموثوقية.
6. القوالب تحتوي على قواعد وشروط وليست مجرد نسخ للعناصر.
7. قابلية التطبيق (Applicability) هي جوهر الفلترة.
8. الذكاء الاصطناعي مساعد تصنيف وليس الحكم النهائي.

---

## 12. تعريف المنتج القابل للتطبيق (MVP - Phase 1)

النسخة الأولى (المرحلة الأولى من خارطة الطريق) يجب أن تركز بإتقان تام على:
1. استيراد الكتالوجات الحالية.
2. تحويل كل عنصر إلى security_artifacts وتصنيفه.
3. واجهة عرض الكتالوج مع فلاتر (USACM).
4. إنشاء ملف مؤسسي (Profile) واختيار قالب أولي.
5. شاشة تقييم حالة التطبيق وإضافة الأدلة والملاحظات.
6. لوحة تقدم (Dashboard) أساسية تعرض الفجوات والإنجاز.
